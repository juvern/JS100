// multiplyNumbers is the function name and the below is the function declaration
function multiplyNumbers(num1, num2, num3) { // parameters
  // function body
  let result = num1 * num2 * num3; 
  return result;
}

// arguments are 2, 3 and 4
let product = multiplyNumbers(2, 3, 4); // function invocation


/* The names of all variables in this program

This code's complete list of variables includes multiplyNumbers, num1, num2, num3, result, and product. 
In particular, the function name and its parameters are all variables. 
In this case, multiplyNumbers is a global variable, but num1, num2, and num3 are local variables defined inside the function.

*/